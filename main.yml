---
- hosts: all
  become: yes
  roles:
    - git
    - neovim
    - fish
    - files
  post_tasks:
    - name: Remove config files
      ansible.builtin.file:
        path: /tmp/ansible_dl
        state: absent
